# Tree Brain / Codex 工作规则

本文件只保存稳定执行规则。动态项目状态见 `docs/CURRENT_PROJECT_STATE.md`。

1. 开始任何任务前，先读取本文件和 `docs/CURRENT_PROJECT_STATE.md`。
2. 已有真实验收证据的能力视为冻结；没有新的失败证据，不得重新开发、重新扫描或重新验收。
3. 不改变 Owner Goal（用户最终目标），不把工程手段升级成新的产品目标。
4. 外部产品、账号权限、平台能力必须先做最小能力验证，再投入完整工程。
5. 新路线先做最小可行验证；关键前提不成立时立即停止并记录，不继续扩建。
6. 已失败或已被外部条件阻塞的路线，没有新证据、产品变化或 Owner 明确要求，不得原样重试。
7. Tree Brain 的持久任务状态是 Codex 任务事实的权威来源；镜像、文档和 GitHub 文件不得反向成为第二套任务状态机。
8. 保留原 `thread_id` 恢复语义；已有线程必须 `resumeThread`，禁止用 `startThread` 代替恢复。
9. 不重做已通过的 Control Plane（控制平面）、Worker（执行进程）、线程恢复、持久任务状态和 Content Reader（内容读取器）链路，除非出现新的回归证据。
10. 只有遇到登录、凭据、授权、付费、提升权限或不可逆操作时才要求 Owner 手动介入。
11. 每次任务结束必须留下可验证证据，并更新当前项目状态；不能只用“看起来完成”代替真实验收。
12. 当前被明确关闭的路线：本地聊天迁移、继续扩建 Tunnel（安全隧道）、重新设计 OAuth（开放授权）、GitHub 第二套控制总线。只有条件发生实质变化时才能重开。
