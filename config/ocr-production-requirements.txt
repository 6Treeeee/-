# Exact dependency closure resolved by the 2026-09-23 Linux feasibility build.
# Use --no-deps: replace the GUI OpenCV wheel with its same-version headless build.
# Real deployed GUI-wheel import failed with missing libxcb.so.1; no OCR changes.
flatbuffers==25.12.19
numpy==2.5.3
onnxruntime==1.30.0
opencv-python-headless==5.0.0.93
packaging==26.3
pillow==12.3.0
protobuf==7.36.2
pyclipper==1.4.0
PyYAML==6.0.3
rapidocr-onnxruntime==1.4.4
shapely==2.1.2
six==1.17.0
tqdm==4.70.1
