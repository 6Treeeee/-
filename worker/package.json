{
  "name": "content-reader-a2a-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.3.0"
  },
  "scripts": {
    "check": "node --check index.mjs && node --check lib/signed-client.mjs && node --check lib/codex-runner.mjs && node --check lib/redact.mjs",
    "test": "node --test test/*.test.mjs",
    "start": "node index.mjs --config config.json --poll"
  },
  "dependencies": {
    "@openai/codex-sdk": "0.149.1"
  }
}
